{
    "version": 2,
    "builds": [
        {
            "src": "api.py",
            "use": "@vercel/python",
            "config": {
                "buildCommand": "bash install_chrome.sh && pip install -r requirements.txt"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api.py"
        }
    ],
    "crons": [
        {
            "path": "/api/cron",
            "schedule": "30 3 * * *"
        }
    ]
}
