{
  "version": 2,
  "builds": [
    {
      "src": "api.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/cron",
      "dest": "/api.py"
    }
  ],
  "hooks": {
    "build": "python3 -m playwright install && python3 -m playwright install-deps"
  }
}
